# song-detect-pi
 song-detect-pi is a Python script that runs on a Raspberry Pi (or any computer) and listens for music using a microphone. When a song is recognized using the Shazam API, it outputs a success message in the terminal. Great for prototyping audio-based triggers and interactive IoT projects.
